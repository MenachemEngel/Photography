<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.photography">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:usesCleartextTraffic="true"
        android:supportsRtl="true"
        android:theme="@style/AppTheme2"
        android:largeHeap="true">
        <activity android:name=".activities.EmailActivity"
            android:noHistory="true"/>
        <activity android:name=".activities.LoginActivity"
            android:noHistory="true"/>
        <activity android:name=".activities.RegisterActivity"
            android:noHistory="true"/>
        <activity android:name=".activities.OwnerActivity1"
            android:noHistory="true"/>
        <activity android:name=".activities.OwnerActivity2"/>
        <activity android:name=".activities.OwnerActivity" />
        <activity android:name=".activities.GuestActivity2"/>
        <activity android:name=".activities.GuestActivity1"
            android:noHistory="true"/>
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>